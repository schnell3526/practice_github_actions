name: Log group
on: push
jobs:
    log:
        runs-on: ubuntu-latest
        steps:
            - run: |
                edcho "::group::Show environment variables"
                printenv
                echo "::endgroup::"
